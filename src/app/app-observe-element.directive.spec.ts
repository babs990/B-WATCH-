import { AppObserveElementDirective } from './hidePanier.directive';

describe('AppObserveElementDirective', () => {
  it('should create an instance', () => {
    const directive = new AppObserveElementDirective();
    expect(directive).toBeTruthy();
  });
});
